{
  "name": "prepaired-beta-worker",
  "version": "1.0.0",
  "description": "PrepAIred beta free tier Worker — rotating house Groq keys",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv:namespace create BETA_RATE"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
